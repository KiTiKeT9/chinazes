# Static assets

Любые файлы тут будут доступны в renderer по URL `/<имя>` (vite public dir).

## РКН-тян

Положи сюда `avatar.png` (любой квадратный PNG, 256×256 идеально) — он автоматически
появится как логотип в боковой панели. Если файла нет, отрисуется анимированный
градиентный кружок-фолбэк.
